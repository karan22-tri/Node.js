<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page</title>
</head>
<body>
    <center>
    <h1>Edit-Data</h1>
    <form action="/update"  method="post">
        <input type="hidden" name="id" value="<%= user.id %>">
        <input type="text" placeholder="Enter your name" name="name" value="<%= user.name %>">
        <input type="text" placeholder="Enter your email" name="email" value="<%= user.email %>">
        <select id="dropdown" name="role" class="form-control" required>
            <option disabled selected value>Select current role</option>
            <option value="student" <%= user.role === 'student' ? 'selected' : '' %>>Student</option>
            <option value="job" <%= user.role === 'job' ? 'selected' : '' %>>Full Time Job</option>
            <option value="learner" <%= user.role === 'learner' ? 'selected' : '' %>>Full Time Learner</option>
            <option value="preferNo" <%= user.role === 'preferNo' ? 'selected' : '' %>>Prefer not to say</option>
            <option value="other" <%= user.role === 'other' ? 'selected' : '' %>>Other</option>
          </select>
        <label>man</label>
        <input type="radio" name="gender" value="man" <%= user.gender === 'man' ? 'checked' : '' %>>
        <label>woman</label>
        <input type="radio" name="gender" value="woman" <%= user.gender === 'woman' ? 'checked' : '' %>>
        <label>others</label>
        <input type="radio" name="gender" value="others" <%= user.gender === 'others' ? 'checked' : '' %>>
        <br>
        <label>Front-end-projects</label>
        <input type="checkbox" name="prefer" value="front-end-projects" <%= user.prefer.includes('front-end-projects') ? 'checked' : '' %>>
        <label>Back-end-projects</label>
        <input type="checkbox" name="prefer" value="back-end-projects" <%= user.prefer.includes('back-end-projects') ? 'checked' : '' %>>
        <label>Data Visualization</label>
        <input type="checkbox" name="prefer" value="data-visualization" <%= user.prefer.includes('data-visualization') ? 'checked' : '' %>>
        <label>Videos</label>
        <input type="checkbox" name="prefer" value="videos" <%= user.prefer.includes('videos') ? 'checked' : '' %>>
        <button type="submit">UPDATE</button>
    </form>
    </center>
</body>
</html>